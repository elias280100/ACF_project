ToDo:

Test Cases in VHDL/Verilog

Test runner script in C
therefore the expected inputs/outputs of other groups are needed

Synthesis:
Input:
Output:

Mapping:
Input:
Output:

Placer:
Input:
Output:

Routing:
Input:
Output:

Timing:
Input:
Output:

Bitstream:
Input:
Output:


Metrics to capture:

Synthesis:
-number of gates
-number if FFs inferred
-logic depth (max levels)
-time

Mapping:
-number of LUTs used
-carry chains used
-time

Placement:
-total wirelength
-number of CLBs used
-max bounding box (width x length)
-placement density
-time

Routing:
number of routed nets / total nets
-total routed wire length
-channel width (tracks needed)
-routing congestion
-time

Timing:
critical path delay
-slack
number of timing violations
-Clock frequency

Bitstream:
-size (Bytes)
-number of configuration bits set
-compression ratio
-time





Individual Test cases from easy to medium to challenging for each project based on challenges to deal with
(e. g. for synthesis: complex Boolean logic patterns -> massive XOR structure)
Measure metrics (LUT count, LUT depth, wire length,...) with Open source tools (Yosys, Nextpnr)
Extract metrics from output files and compare it to measured metrics of Open Source tools

So far implemented test cases for synthesis, mapping and placer (e. g. Multiplier (Dadda/Wallace), Cyclic Redundancy Check, Carry Look Ahead Adder,...)
Ongoing implementation of test cases for Routing, Timing and Bitstream; extracting metrics from output files




